name: atlas_cli
description: Command-line and terminal entry point for Atlas.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  args: ^2.7.0
  atlas_acp: ^0.1.0
  atlas_mcp: ^0.1.0
  atlas_provider: ^0.1.0
  atlas_runtime: ^0.1.0
  atlas_storage: ^0.1.0
  atlas_tools: ^0.1.0
  atlas_tui: ^0.1.0
  atlas_ws: ^0.1.0
  logging: ^1.3.0
