name: atlas_cli
description: Command-line and terminal entry point for Atlas.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace
dependencies:
  atlas_acp: ^0.1.0
  atlas_config: ^0.1.0
  atlas_prompt: ^0.1.0
  atlas_provider: ^0.1.0
  atlas_runtime: ^0.1.0
  atlas_storage: ^0.1.0
  atlas_tools: ^0.1.0
  atlas_tui: ^0.1.0
dev_dependencies:
  test: ^1.31.0
