name: atlas_cli
description: Command-line and terminal entry point for Atlas.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  args: ^2.7.0
  atlas_daemon: ^0.1.0
  atlas_protocol: ^0.1.0
  atlas_tui: ^0.1.0
