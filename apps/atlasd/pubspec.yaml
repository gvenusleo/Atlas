name: atlas_daemon
description: Local runtime host for Atlas clients.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  atlas_acp: ^0.1.0
  atlas_mcp: ^0.1.0
  atlas_protocol: ^0.1.0
  atlas_provider: ^0.1.0
  atlas_runtime: ^0.1.0
  atlas_storage: ^0.1.0
  atlas_tools: ^0.1.0
  logging: ^1.3.0
  shelf: ^1.4.2
  shelf_web_socket: ^3.0.0
