# atlasd

The local Atlas runtime host and WebSocket service.

This application will compose the runtime, storage, providers, tools, ACP, MCP, and client protocol packages. No executable is implemented yet.
