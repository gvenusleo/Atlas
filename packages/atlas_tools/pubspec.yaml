name: atlas_tools
description: Built-in tool implementations for Atlas.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  atlas_runtime: ^0.1.0
  file: ^7.0.1
  logging: ^1.3.0
  path: ^1.9.1
