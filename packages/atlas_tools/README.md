# atlas_tools

Built-in Atlas tool implementations such as file operations and shell execution.

Tools implement ports from `atlas_runtime` and expose structured calls and results; they do not format output for Flutter, TUI, ACP, or MCP clients.
