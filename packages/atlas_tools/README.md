# atlas_tools

Built-in Atlas tool implementations such as file operations and shell execution.

Tools expose structured calls and results through `atlas_core`; they do not format output for Flutter, TUI, ACP, or MCP clients.
