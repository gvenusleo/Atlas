name: atlas_acp
description: Agent Client Protocol adapter for Atlas.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace
dependencies:
  atlas_runtime: ^0.1.0
  json_rpc_2: ^4.1.0
  stream_channel: ^2.1.4
dev_dependencies:
  atlas_tools:
    path: ../atlas_tools
  test: ^1.31.0
