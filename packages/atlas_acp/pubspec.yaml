name: atlas_acp
description: Agent Client Protocol adapter for Atlas.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace
dependencies:
  acpd: ^1.0.0
  acpd_io: ^1.0.0
  atlas_runtime: ^0.1.0
  stream_channel: ^2.1.4
dev_dependencies:
  test: ^1.31.0
