name: atlas_ws
description: Versioned WebSocket wire contract and transport for remote Atlas clients.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace
dependencies:
  atlas_acp: ^0.1.0
  atlas_runtime: ^0.1.0
  crypto: ^3.0.0
  shelf: ^1.4.0
  shelf_web_socket: ^3.0.0
  stream_channel: ^2.1.0
  web_socket_channel: ^3.0.0
dev_dependencies:
  test: ^1.31.0
