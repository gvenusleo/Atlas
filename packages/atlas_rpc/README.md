# atlas_rpc

Reusable JSON-RPC 2.0 transport, request correlation, notification, cancellation, and error handling.

ACP- and MCP-specific methods, capabilities, and lifecycle rules belong in their protocol packages.
