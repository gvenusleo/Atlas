name: atlas_storage
description: Drift persistence for Atlas threads and runs.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  atlas_runtime: ^0.1.0
  drift: ^2.34.3
  logging: ^1.3.0
  path: ^1.9.1
