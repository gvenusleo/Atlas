name: atlas_storage
description: SQLite persistence for Atlas threads and runs.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  atlas_core: ^0.1.0
  logging: ^1.3.0
  path: ^1.9.1
  sqlite_async: ^0.14.4
