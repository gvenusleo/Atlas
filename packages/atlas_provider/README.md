# atlas_provider

Model provider adapters and provider-specific request/response conversion.

Provider authentication, endpoints, and wire formats stay here. Provider-specific fields must not leak into `atlas_core` request types.
