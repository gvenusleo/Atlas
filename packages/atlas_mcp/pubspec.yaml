name: atlas_mcp
description: Model Context Protocol adapter for Atlas.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  atlas_core: ^0.1.0
  atlas_rpc: ^0.1.0
  http: ^1.6.0
  web_socket_channel: ^3.0.3
