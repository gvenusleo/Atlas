name: atlas_config
description: YAML configuration loading for Atlas composition roots.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace
dependencies:
  atlas_provider: ^0.1.0
  atlas_runtime: ^0.1.0
  yaml: ^3.1.3
dev_dependencies:
  test: ^1.31.0
