# atlas_core

Stable domain models, events, and ports shared by the Atlas runtime and its adapters.

This package must remain independent of Flutter, persistence engines, network clients, model providers, and protocol transports.
