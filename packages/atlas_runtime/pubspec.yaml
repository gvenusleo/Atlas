name: atlas_runtime
description: The shared Atlas agent runtime and orchestration layer.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  atlas_core: ^0.1.0
  logging: ^1.3.0
  uuid: ^4.6.0
